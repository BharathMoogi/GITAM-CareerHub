# ─────────────────────────────────────────────────────────────────────────────
# GITAM CareerHub — Production requirements.txt
# All dependencies pinned to minor version for reproducible builds
# ─────────────────────────────────────────────────────────────────────────────

# ── FastAPI Core ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
uvloop>=0.19.0
httptools>=0.6.1
starlette>=0.36.0
python-multipart>=0.0.9

# ── Database & ORM ────────────────────────────────────────────────────────────
SQLAlchemy>=2.0.28
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.1
aiosqlite>=0.20.0           # SQLite async (testing only)

# ── Pydantic v2 & Settings ───────────────────────────────────────────────────
pydantic>=2.6.4
pydantic-settings>=2.2.1
python-dotenv>=1.0.1
email-validator>=2.1.0

# ── Security & Authentication ─────────────────────────────────────────────────
PyJWT[crypto]>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1
cryptography>=42.0.0

# ── Redis & Caching ───────────────────────────────────────────────────────────
redis[hiredis]>=5.0.3       # Async Redis with hiredis C parser
hiredis>=2.3.2

# ── Celery Background Tasks ───────────────────────────────────────────────────
celery>=5.3.6
kombu>=5.3.3
flower>=2.0.1               # Celery monitoring UI (optional)

# ── Template Engine ───────────────────────────────────────────────────────────
Jinja2>=3.1.3

# ── AI / LLM Providers (stubs — install only when keys are configured) ────────
# openai>=1.14.0
# anthropic>=0.25.0
# google-generativeai>=0.4.0
# groq>=0.4.2

# ── Monitoring & Observability ────────────────────────────────────────────────
prometheus-client>=0.20.0
opentelemetry-api>=1.23.0
opentelemetry-sdk>=1.23.0
opentelemetry-instrumentation-fastapi>=0.44b0
opentelemetry-instrumentation-sqlalchemy>=0.44b0
opentelemetry-exporter-otlp>=1.23.0

# ── HTTP Client (AI provider calls, webhooks) ─────────────────────────────────
httpx>=0.27.0
aiohttp>=3.9.3

# ── File Handling (Excel export, CSV import) ──────────────────────────────────
openpyxl>=3.1.2
python-csv-utils>=0.1.2

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.1.1
pytest-asyncio>=0.23.6
pytest-cov>=5.0.0
httpx>=0.27.0

# ── Code Quality (dev only — not needed in production image) ──────────────────
# ruff>=0.3.4
# black>=24.3.0
# bandit[toml]>=1.7.7
# mypy>=1.9.0
